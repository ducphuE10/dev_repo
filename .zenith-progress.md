# Zenith Progress Log

## Codebase Patterns
<!-- Consolidated knowledge from all iterations. Read this FIRST. -->
- `.zenith-progress.md` is the canonical append-only implementation log; do not reintroduce `PROGRESS.txt`.
- The intended stack is TypeScript throughout with pnpm workspaces and Turborepo coordinating `apps/api`, `apps/mobile`, `apps/web`, and shared `packages/*`.
- The workspace namespace is `@dupe-hunt/*`, with root-level `pnpm lint`, `pnpm typecheck`, and `pnpm test` serving as the stable quality entry points.
- Early bootstrap verification lives under `scripts/`, while app-level `dev` commands can stay as lightweight long-running stubs until the framework-specific implementations land.
- Shared object contracts belong in `packages/types`, shared database code belongs in `packages/db`, and apps must not import directly from each other.
- Shared TypeScript defaults live in `packages/config/typescript/base.json`, with the root `tsconfig.json` extending that base and owning workspace-specific `paths` mappings.
- Environment contracts are centralized in `packages/config/src/index.ts`, while each app keeps a matching `.env.example` file as the source-of-truth template for local setup.
- `packages/db` owns the schema layer and exports a `createDatabaseClient` helper plus Drizzle table definitions from TypeScript source without requiring a build step.
- Database migrations live as ordered SQL files in `packages/db/migrations`, and the repo-standard way to apply them is `pnpm db:migrate`, which delegates to `packages/db/scripts/migrate.mjs`.
- The migration runner resolves `DATABASE_URL` from `apps/api/.env`, root `.env`, or the active shell, so local infra and API config stay aligned.
- Local infrastructure is defined in `docker-compose.yml` with PostgreSQL 16 and Redis 7 defaults that match the API example environment file.
- Docker Compose host ports are overridable with `POSTGRES_PORT` and `REDIS_PORT`, which keeps the documented local workflow usable on machines where `5432` or `6379` is already taken.
- Always use `IF NOT EXISTS` for migration SQL statements.
- Fastify API routes are expected to use Zod validation, consistent `{ error: { code, message } }` responses, Drizzle for database access, and BullMQ for heavy async work.
- Mobile screens should stay thin: screen files in `screens/`, reusable UI in `components/`, business logic in custom `hooks/`, React Query for server state, and SecureStore only for auth tokens.
- Next.js web pages are browse-only in v1, prefer server-side data fetching for SEO, and must provide metadata for every public route.
- Feed queries must always filter `status = 'active'` and never return flagged or removed posts.
- Affiliate click tracking happens server-side in the redirect endpoint, never in the frontend.
- Receipt images live in the private R2 bucket only and must never be exposed to clients.
- Background worker jobs handle heavy async work such as video processing, OCR, affiliate wrapping, and aggregation counter updates.

---

## [2026-04-02 09:00] - Project Kickoff / Design & Planning

### What was implemented
- Full product brainstorming session identifying Dupe Hunt as the core concept.
- Competitive landscape research across Dupe.com, Brandefy, SkinSort, and Skinskool.
- Core product decisions finalized for platform, categories, content format, monetization, verification, and discovery.

### Files changed
- `docs/design.md` — full product design spec covering vision, features, monetization, growth, KPIs, and MVP scope.
- `docs/technical.md` — database schema, API endpoints, mobile screen map, affiliate flow, and receipt verification details.
- `docs/plan.md` — 10-phase implementation plan with task checklists.
- `README.md` — project overview, stack, repo structure, and getting started guidance.
- `DEV_RULES.md` — contribution rules for developers and AI agents.

### Learnings for future iterations
- The core differentiator versus competitors is community trust through real people and verified purchases.
- Multi-category from day one requires strong category-filtering UX so the feed stays coherent.
- The v1 "For You" tab is category-filtered and upvote-ranked, not ML-driven.
- Affiliate revenue starts on day one, while brand analytics is a later B2B expansion.
- Receipt images are privacy-sensitive assets and must remain private-bucket-only.

---

## [2026-04-03 08:33] - Iteration 1 / Planning Baseline

### What was implemented
- Audited the repository to confirm the branch state before implementation work.
- Created `.zenith-plan.json` with dependency-ordered implementation subtasks for the MVP.
- Started `.zenith-progress.md` as the consolidated codebase-pattern and iteration log.

### Files changed
- `.zenith-plan.json` — iteration plan, discovered issues, and the initial quality command inventory.
- `.zenith-progress.md` — first-pass codebase patterns and architectural constraints.

### Learnings for future iterations
- Workspace bootstrap is the first real implementation task because the repo initially lacked package-manager and app scaffolding.
- Quality commands only become meaningful after the workspace scripts and tool configuration exist.
- The docs are internally consistent enough to drive implementation, but the README initially described a future state rather than the checked-in state.

---

## [2026-04-03 08:28] - Iteration 2 / ST-1 Workspace Bootstrap

### What was implemented
- Bootstrapped the root pnpm workspace with `package.json`, `pnpm-workspace.yaml`, `turbo.json`, `tsconfig.json`, and `pnpm-lock.yaml`.
- Scaffolded the documented `apps/api`, `apps/web`, `apps/mobile`, `packages/config`, `packages/types`, and `packages/db` directories with minimal manifests and TypeScript entry files.
- Added lightweight workspace verification scripts so `pnpm lint`, `pnpm typecheck`, and `pnpm test` are real commands instead of placeholders.
- Added app-level `dev` stubs so `pnpm dev` launches all three application workspaces during bootstrap.

### Files changed
- `.gitignore` — ignores workspace-generated artifacts and local environment files.
- `package.json`, `pnpm-workspace.yaml`, `turbo.json`, `tsconfig.json`, `pnpm-lock.yaml` — root monorepo bootstrap and toolchain wiring.
- `apps/*`, `packages/*` — initial workspace manifests and placeholder TypeScript entry points.
- `scripts/dev-stub.mjs`, `scripts/lint-workspace.mjs`, `scripts/workspace.test.mjs` — bootstrap dev and verification helpers.
- `.zenith-plan.json`, `.zenith-progress.md` — iteration tracking updates.

### Verification results
- `pnpm lint` — pass
- `pnpm typecheck` — pass
- `pnpm test` — pass
- `timeout 3s pnpm dev` — pass as a smoke check for stub startup; intentionally interrupted after confirming all three app stubs start

### Learnings for future iterations
- The workspace namespace and package boundaries are now established, so later iterations can add shared config and real framework dependencies without reworking the repo shape.
- Root quality commands are now available and should be kept current in `.zenith-plan.json` as the scaffold becomes more specialized.
- A bounded `pnpm dev` smoke check is sufficient at bootstrap time to verify workspace orchestration before real app runtimes exist.

---

## [2026-04-03 10:33] - Iteration 3 / ST-2 Shared Config, Types, and Environment Contracts

### What was implemented
- Replaced the placeholder shared packages with reusable workspace types plus centralized environment-contract definitions for the API, web, and mobile apps.
- Added shared TypeScript defaults under `packages/config/typescript/base.json` and updated the root `tsconfig.json` to consume that shared baseline with workspace package path mappings.
- Added concrete `.env.example` files for all three apps and expanded the workspace lint/test scripts so these shared config artifacts are verified automatically.

### Files changed
- `packages/config/*` — shared TypeScript defaults and environment-contract exports.
- `packages/types/*` — reusable workspace types.
- `apps/api/.env.example`, `apps/web/.env.example`, `apps/mobile/.env.example` — source-of-truth local setup templates.
- `tsconfig.json`, `scripts/*` — workspace verification updates to cover the new shared config assets.
- `.zenith-plan.json`, `.zenith-progress.md` — iteration tracking updates.

### Verification results
- `pnpm lint` — pass
- `pnpm typecheck` — pass
- `pnpm test` — pass

### Learnings for future iterations
- Shared contracts can stay dependency-light for now by using TypeScript types plus file-based verification, which avoids bringing in runtime validation libraries before the API app exists.
- The API scaffold can consume `packages/config` directly for environment parsing in the next iteration instead of inventing app-local config helpers.

---

## [2026-04-03 10:48] - Iteration 4 / ST-3 Database Package and Local Infra

### What was implemented
- Replaced the placeholder `packages/db` package with Drizzle schema definitions covering the MVP tables from `docs/technical.md`.
- Added sequential SQL migrations plus seeded launch categories and a Node-based migration runner exposed as `pnpm db:migrate`.
- Added local PostgreSQL and Redis infrastructure via `docker-compose.yml`, updated repo checks to validate the new artifacts, and corrected the plan/discovered-issues state to reflect the real branch.

### Files changed
- `packages/db/*` — Drizzle schema/client exports, migration runner, and ordered SQL migrations.
- `package.json`, `tsconfig.json` — root database workflow command and workspace path mapping.
- `scripts/lint-workspace.mjs`, `scripts/workspace.test.mjs` — quality checks for the database package and local infra.
- `docker-compose.yml`, `README.md` — local development infrastructure and current-state documentation.
- `.zenith-plan.json`, `.zenith-progress.md` — iteration tracking updates.

### Learnings for future iterations
- The repo now has a real database foundation, so ST-4 can wire Fastify directly to `@dupe-hunt/db` instead of inventing app-local schema definitions.
- Keeping migrations as checked-in SQL plus a minimal Node runner avoids adding a build step before the API runtime exists.
- The README should keep distinguishing implemented infrastructure from planned framework work so setup instructions stay trustworthy.
- Local infra defaults should remain overridable because shared dev environments commonly already occupy Redis or Postgres default host ports.
